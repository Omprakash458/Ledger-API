---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: operator-binding
  namespace: default

subjects:
- kind: User
  name: operator

roleRef:
  kind: Role
  name: operator-role
  apiGroup: rbac.authorization.k8s.io
